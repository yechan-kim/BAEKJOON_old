{
    "version": "2.0.0",
   "runner": "terminal",
   "type": "shell",
   "echoCommand": true,
   "presentation" : { "reveal": "always" },
    "tasks" : [
        {
            "label": "execute",
            "command": ["gcc ${fileDirname}/${fileBasenameNoExtension}.c -o ${fileDirname}/${fileBasenameNoExtension}.out\n", "${fileDirname}/${fileBasenameNoExtension}.out\n", "rm ${fileDirname}/${fileBasenameNoExtension}.out"],
            "group": "test"
        }
    ]
}