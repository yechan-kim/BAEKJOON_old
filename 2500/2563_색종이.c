#include <stdio.h>

int main()
{
    int base[101][101] = {0};
    int i, j, a = 0, b = 0, c = 0;
    scanf("%d", &c);
    for (i = 0; i < c; i++)
    {
        scanf("%d %d", &a, &b);
        
    }
}